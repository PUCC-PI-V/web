# Projeto Integrador V: Web
> 
## Como executar? 
  - Primeiramente, instale as dependências com o comando `npm install`.
  - Para executar, execute o comando `npm run dev`.

>
## Edição
  - Para criar componentes, crie o arquivo na pasta `/components`.